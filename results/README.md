# Results

The full experiment writes its outputs under this directory.

This clean repository package does not fabricate missing per-seed results.
After running the full 20-seed experiment, commit the genuine generated
CSV/JSON summary and raw result files if they are to support the manuscript's
reproducibility statement.

Large worker caches and saved model binaries are ignored by `.gitignore`.
