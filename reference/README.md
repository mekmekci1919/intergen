# Manuscript reference values

`manuscript_reported_results.csv` contains selected **reported summary values**
copied from the revised manuscript so that generated outputs can be checked
against the submitted paper.

These values are **not raw per-seed observations** and must not be used to
construct synthetic seed-level results. The actual raw CSV/JSON files should
be generated by `intergen_experiment.py`.
